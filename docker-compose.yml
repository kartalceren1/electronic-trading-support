services:

  postgres:

    image: postgres:16

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: trading123
      POSTGRES_DB: trading_support

    ports:
      - "5432:5432"


  app:

    build: .

    ports:
      - "5000:5000"

    depends_on:
      - postgres